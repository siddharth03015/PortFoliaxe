// Seed script is no longer used in multi-user mode.
// Users register and create their own data.
const seedDatabase = async () => {
  console.log('ℹ️  Multi-user mode — no seed data needed.');
};

module.exports = { seedDatabase };
